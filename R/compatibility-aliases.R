# Canonical snake-case API. Assignments intentionally preserve the exact
# formals, return values, and behaviour of the legacy functions.

exclude_studies_by_name <- exclude_by_names
rename_count_columns <- rename
rename_prefixed_count_columns <- rename.first
heterogeneity_string <- het.string
reitsma_subgroup_comparisons <- reitsma.subgroup.comparisons
forest_diag <- forest.diag
forest_diag_combined <- forest.diag.combined
forest_diag_subgroup <- forest.diag.subgroup
forest_diag_subgroup_combined <- forest.diag.subgroup.combined
auc_bootstrap <- AUC_bootstrap
multiple_srocs <- multiple.srocs
dta_outliers <- dta.outliers
forest_diag_no <- forest.diag.no
dta_outliers_multi <- dta.outliers.multi
forest_diag_subgroup_no <- forest.diag.subgroup.no
multiple_srocs_no <- multiple.srocs.no
pbs3 <- PBS3
mvpbt_bootstrap <- MVPBT_bootstrap
mvpbt_boot <- MVPBT_boot
pubbias_diag <- pubbias.diag
multiple_lr_mats <- multiple.LRmats
df_mixer <- df.mixer

